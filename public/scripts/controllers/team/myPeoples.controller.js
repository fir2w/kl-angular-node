define(['angular'], function (angular) {
  'use strict';

  angular.module('klwork.controllers')
    .controller('myPeoplesController', function ($scope, $http) {
     /* var tempResponse = $http.get('/myPeoples');
      tempResponse.success(function (data, status, headers, config) {
        $scope.users = data;
        console.log(data);
      });

      $scope.detail = {name: 'kerry'};
      $scope.isDialogShow = false;

      $scope.addMember = function () {
        $scope.detail = {name: ''};
        $scope.isDialogShow = true;
      };

      $scope.close = function () {
        $scope.isDialogShow = false;
      };*/
    });
});